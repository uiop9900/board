spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3310/board?serverTimezone=UTC
#    url: jdbc:mysql://mysql-local:3306/board TODO: 도커로 띄우기
    username: root
    password: root123

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect # ??? ??? ??? ????.
    hibernate:
      ddl-auto: validate # TODO: 초기 db 넣기
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6380

  sql:
    init:
      data-locations: classpath*:db/mysql/data.sql

logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG

server:
  servlet:
    encoding:
      force-response: true

jwt:
  secret-key: dGhpcyBpcyBhIHNlY3JldCBzZWNyZXQga2V5IGZvciBKV1Qgc3RyaW5nIG1lYW41
